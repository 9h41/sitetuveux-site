{
  "name": "sitetuveux-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static 11ty migration for sitetuveux.com",
  "scripts": {
    "clean": "rm -rf _site",
    "build": "npm run clean && npx @11ty/eleventy",
    "start": "npx @11ty/eleventy --serve --port=8080"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "playwright": "^1.58.2"
  }
}
